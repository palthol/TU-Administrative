# Base URL of the admin API service (defaults to http://localhost:3001 if unset)
VITE_API_BASE_URL=http://localhost:3001

# The receipts app asks the operator for the admin API key at runtime. Do not put the key
# in a VITE_* variable because Vite variables are included in the browser bundle.
